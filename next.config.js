/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    experimental: {
    },
    devIndicators: {
      errorOverlay: false,
    },
  };
  
  module.exports = nextConfig;
  